@page "/books2"




@using Acme.BookStore.Books.Dtos
@using Volo.Abp.Application.Dtos
@using Acme.BookStore.Books
@using Acme.BookStore.Localization
@using Microsoft.Extensions.Localization
@using Volo.Abp.AspNetCore.Components.Web

@using Syncfusion.Blazor.Cards
@using Syncfusion.Blazor.Buttons
@using Syncfusion.Blazor.Grids
@inject IStringLocalizer<BookStoreResource> L
@inject AbpBlazorMessageLocalizerHelper<BookStoreResource> LH
@inherits AbpCrudPageBase<IBookAppService, BookDto, Guid, PagedAndSortedResultRequestDto, CreateUpdateBookDto>


<Row Class="justify-content-between">
    <Column ColumnSize="ColumnSize.IsAuto">
        <h2>@L["Books"]</h2>
    </Column>
    <Column ColumnSize="ColumnSize.IsAuto">
        <Button Color="Color.Primary"
                Clicked="OpenCreateModalAsync">
            @L["NewBook"]
        </Button>
    </Column>
</Row>
<SfCard ID="BasicCard">
  @*   <CardHeader>
        <CardHeaderContent>

        </CardHeaderContent>
       
    </CardHeader> *@
    <CardContent >

        <SfGrid DataSource="@Entities" AllowResizing="true" AllowSorting="true" AllowPaging="true" EnableHover="true" AllowSelection="true" EnableAdaptiveUI="true" AllowTextWrap="true" RowRenderingMode="RowDirection.Vertical" Width="100%" Height="100%">
 @*            <GridSortSettings>
                <GridSortColumns>
                    <GridSortColumn Field=@nameof(BookDto.Name) Direction="SortDirection.Ascending"></GridSortColumn>
                </GridSortColumns>
            </GridSortSettings> *@
            @*<GridPageSettings PageCount="3" PageSizes="true"></GridPageSettings>*@
            <GridColumns>
                @* <GridColumn Field=@nameof(BookDto.ID) HeaderText="ID" TextAlign="TextAlign.Left"></GridColumn> *@
                <GridColumn Field=@nameof(BookDto.Name) HeaderText="Name" TextAlign="TextAlign.Left"></GridColumn>
                <GridColumn Field=@nameof(BookDto.Type) HeaderText="Type" TextAlign="TextAlign.Left"></GridColumn>
                <GridColumn Field=@nameof(BookDto.PublishDate) HeaderText="Publish Date" TextAlign="TextAlign.Left"></GridColumn>
                <GridColumn Field=@nameof(BookDto.Price) HeaderText="Price" TextAlign="TextAlign.Left"></GridColumn>
                <GridColumn Field=@nameof(BookDto.CreationTime) HeaderText="Creation Time" TextAlign="TextAlign.Left"></GridColumn>
                </GridColumns>
        </SfGrid>

    </CardContent>
    @* <CardFooter>
        <CardFooterContent>
            <SfButton CssClass="e-btn e-outline e-primary">Read More</SfButton>
        </CardFooterContent>
    </CardFooter> *@
</SfCard>


@code {
    protected override async Task OnInitializedAsync()
    {
        // await OnDataGridReadAsync();
    }
}
